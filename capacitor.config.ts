import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gbggold.app',
  appName: 'Golf Score',
  webDir: 'dist/golf-score-app/browser',
};

export default config;
